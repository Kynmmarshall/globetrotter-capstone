# Copy this to .env on the VPS (next to docker-compose.yml) and fill in real,
# randomly generated passwords - never commit the real .env.
# Generate strong values with: openssl rand -base64 32

MATOMO_DB_ROOT_PASSWORD=
MATOMO_DB_PASSWORD=
